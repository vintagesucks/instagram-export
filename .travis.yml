language: php

php:
  - '8.0'
  - '7.4'
  - '7.3'
  - nightly

matrix:
  allow_failures:
    - php: nightly

before_script:
  - composer install --no-interaction

script:
  - find app -name "*.php" | xargs -n 1 php -l

cache:
  directories:
  - "$HOME/.composer/cache"
